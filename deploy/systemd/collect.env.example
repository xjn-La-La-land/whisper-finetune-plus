# CareSpeech 采集后端机密 / 环境变量
# 用法：拷到机器上的 /etc/carespeech/collect.env，填入真实值，并 chmod 600。
#   sudo mkdir -p /etc/carespeech
#   sudo cp deploy/systemd/collect.env.example /etc/carespeech/collect.env
#   sudo nano /etc/carespeech/collect.env      # 填入真实 JWT_SECRET_KEY
#   sudo chmod 600 /etc/carespeech/collect.env
#
# ⚠️ 本文件是模板，可入库；填了真实密钥的 /etc/carespeech/collect.env 千万别入库。
#
# systemd EnvironmentFile 语法注意：每行 KEY=VALUE，不要写 `export`，值不要加引号。

# 登录态 token 的签名密钥，必须长期固定（换了会把所有已登录用户踢回登录页）。
# 生成一次后存进密码管理器，长期复用同一个值：
#   python -c "import secrets; print(secrets.token_urlsafe(32))"
JWT_SECRET_KEY=在这里粘贴你长期固定的密钥
