# 配置模板：复制为 .env 后填入真实值 ——  cp .env.example .env
# .env 已被 .gitignore 忽略，真实 token 不会入库（本模板只放占位符，可入库）。
# 加载方式：启动 uvicorn 前执行   set -a && source .env && set +a
#
# 网页端「管理微调好的模型 → 上传到 ModelScope」按钮上传 LoRA 权重用：
MODELSCOPE_TOKEN=<你的 ModelScope 访问令牌>
MODELSCOPE_LORA_REPO=<owner>/<repo>      # LoRA 落到该仓库的 <用户名>/<模型名>/ 子目录，不存在会自动创建
